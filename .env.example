# Copy to .env and fill in with your own values (do not commit .env)
# Example placeholders shown below – replace them in your local .env only
DISCORD_TOKEN=
CLIENT_ID=
# Optional: set to a single guild ID for faster command registration during development
GUILD_ID=
# Comma-separated Discord user IDs allowed to use /jolly and /grinch
AUTHORIZED_USER_IDS=
